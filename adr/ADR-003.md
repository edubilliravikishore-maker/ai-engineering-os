# ADR-003: Foundation v1 Domain Model & Lifecycle Clarifications

## Status

**Accepted — 2026-08-25. §3.11 amended 2026-08-29.**

§3.1–§3.10 record the Builder's rulings on the ambiguities discovered during Checkpoint 2 and are approved as recorded. §3.11–§3.14 record a later, separate set of Builder rulings, made on 2026-08-25 following the Builder review of the questions this ADR had left explicitly undecided.

**§3.11 was amended on 2026-08-29** by Builder ruling, to remove an internal contradiction discovered during the Checkpoint 3 audit: the *Unresolvable scope* and *Out-of-scope defects* paragraphs classified a different-Feature association in two incompatible ways. The amendment is recorded inline in §3.11 and in [ADR-004 4.14](ADR-004.md#414-different-feature-is-out-of-scope--adr-003-311-contradiction-resolved). **No other ruling in this ADR is altered.**

The underlying rulings have been made by the Builder. This ADR records them.

---

## Context

Design Sessions 001–009 established and froze the architecture of **AI Engineering OS**. [ADR-001](ADR-001.md) established independent verification. [ADR-002](ADR-002.md) established the technology foundation. The [Implementation Blueprint](../docs/02-implementation/Implementation-Blueprint.md) translated that frozen architecture into a concrete engineering plan.

Checkpoint 2 of that blueprint implemented the pure domain layer (`domain/`) and the deterministic lifecycle state machines (`state/`).

Turning the architecture into executable domain concepts surfaced a set of points where the authoritative documents were **silent, underspecified, or internally inconsistent**. None of these are architectural disagreements. They are places where an implementer must choose something, and where choosing silently would have amounted to inventing architecture.

This ADR records the Builder's resolution of those points.

**This ADR is a clarification record. It does not replace, restate, or redesign the architecture.**

---

## Problem — Ambiguities Discovered During Checkpoint 2

Ten items required a decision before Checkpoint 2 could be committed:

| # | Ambiguity | Nature |
| :--- | :--- | :--- |
| 1 | `TaskRevision.status: RevisionStatus` declared with no enumerated members | Gap |
| 2 | `TaskRevision.review_decision_id` references an entity that is never modelled | Gap |
| 3 | Blueprint §6 asserts a `BLOCKED` state that Blueprint §5.1/§5.2 do not define | **Internal inconsistency** |
| 4 | Feature Plan lifecycle has no requester roles and no `SUPERSEDED` entry point | Gap |
| 5 | Work Package lifecycle has states and invariants but no authority table | Gap |
| 6 | QA defect severity/priority scale explicitly left open; defect `status` unenumerated | Deliberate deferral + gap |
| 7 | `Claim.claim_type` and QA `results` have no defined shape | Gap |
| 8 | Feature Plans must express dependency graphs before Task identities exist | Gap |
| 9 | Blueprint §4.1 #9 omits a capability field that Blueprint §5.2 requires | **Internal inconsistency** |
| 10 | DS-009's Coordinator lifecycle is flattened to `Actor.is_active` in the Blueprint | Divergence |

Items 3 and 9 are genuine contradictions **inside the Implementation Blueprint**. Items 6 and 10 touch material the Design Sessions deliberately left open or that Foundation v1 does not yet need. The remainder are omissions.

**§3.11–§3.14 are not part of these ten.** They record a separate Builder review, held on 2026-08-25, of the questions listed under [Questions Not Decided In The Checkpoint 2 Record](#questions-not-decided-in-the-checkpoint-2-record) and of the ADR-001 amendment that Design Session 009 declared owed.

---

## Decisions

Each decision below separates three things explicitly:

- **Existing architectural requirement** — already stated in DS-001–009, ADR-001, ADR-002, or the Blueprint. Not decided here.
- **Clarification** — what this ADR resolves.
- **Explicit deferral** — what Foundation v1 will *not* implement, and what remains owed.

### 3.1 Task Revision Status

**Decision.** Remove the stored `RevisionStatus` from `TaskRevision`. The authoritative active-revision pointer is `Task.active_revision_number`. A revision's active-ness is derived from the Task's pointer and the revision history, never stored on the revision itself.

| | |
| :--- | :--- |
| **Existing architectural requirement** | Task Revisions are strictly additive; history is never overwritten, mutated, or discarded (DS-006; DS-009 "OS as the System of Record"; Blueprint §4.1 #4). At any point in time only one Revision is active (DS-006). `Task.active_revision_number` already exists (Blueprint §4.1 #3). |
| **Clarification** | The `status (RevisionStatus)` attribute declared in Blueprint §4.1 #4 is removed. DS-006's "only one Revision is active" is preserved as a **derived invariant**, not a stored flag. |
| **Explicit deferral** | None. |

**Rationale.** A stored `ACTIVE`/`SUPERSEDED` marker requires writing to an already-recorded revision when the next one is appended. Blueprint §7.1 places `task_revisions` under **append-only history**, so at Checkpoint 4 that becomes an `UPDATE` against a table the architecture declares immutable. Since the Task already owns the active pointer, the stored status is both redundant and in conflict with the immutability invariant.

**Affects.** Blueprint §4.1 #4 and §7.1. Requires code change.

---

### 3.2 ReviewDecision Remains Distinct From Decision

**Decision.** `ReviewDecision` is a distinct domain entity. It is not merged into `Decision`.

- `Decision` — architectural, system-level, and business escalation decisions, with acknowledgements (DS-008).
- `ReviewDecision` — an independent review outcome for one `TaskRevision`: reviewer, outcome (`APPROVED` / `CHANGES_REQUESTED`), and mandatory notes.

| | |
| :--- | :--- |
| **Existing architectural requirement** | `TaskRevision.review_decision_id` exists (Blueprint §4.1 #4). A reviewer outcome with review notes / explicit feedback is mandatory (Blueprint §5.2). Reviewers never modify Worker Revisions; they only produce review decisions (DS-006). |
| **Clarification** | The referenced entity is `ReviewDecision`, distinct from the `Decision` entity of Blueprint §4.1 #8. |
| **Explicit deferral** | None. |

**Rationale.** Merging them would place every routine code review into permanent architectural decision history (DS-009), and would break decision authority, since `REVIEWER` holds authority at no `DecisionScope`.

**Affects.** Blueprint §4.1 (new entity), §4.1 #4, §4.1 #8. No code change — already implemented this way.

---

### 3.3 BLOCKED Deferred From Foundation v1

**Decision.** Foundation v1 state machines define no `BLOCKED` state. The `BLOCKED` reference in the Blueprint §6 event table is a **future capability**, not a current state. The Blueprint requires a correction so it no longer claims `BLOCKED` is a Foundation v1 state.

| | |
| :--- | :--- |
| **Existing architectural requirement** | An Orchestrator decision may invalidate work in progress. The OS must prevent affected Workers from continuing knowingly invalid work; the current work must be preserved; the Coordinator determines whether it resumes, is reused, is abandoned, or is redirected (DS-008). |
| **Clarification** | Blueprint §5.1 and §5.2 are correct as written. Blueprint §6's `EscalationRaised` row incorrectly records a state change to `BLOCKED`. |
| **Explicit deferral** | **Escalation blocking is not implemented in Foundation v1.** The DS-008 requirement above is knowingly unserved in v1 and remains owed. |

**Rationale.** The Foundation v1 vertical slice (Blueprint §10) contains no escalation path, so v1 has no consumer for the state. Adding states to a frozen state machine without a consuming workflow would be over-design. The DS-008 requirement is not withdrawn — it is deferred.

**Constraint.** The future `BLOCKED` capability **must be designed explicitly** — entry states, exit states, and initiating authority — before any implementation.

**Affects.** Blueprint §5.2 (footnote), §6 (correction), §14. No code change.

---

### 3.4 Feature Plan Authority & Lifecycle

**Decision.** The Coordinator owns Feature Plan lifecycle transitions. Foundation v1 defines exactly four transitions:

| From | To | Initiator |
| :--- | :--- | :--- |
| `DRAFT` | `READY` | `COORDINATOR` |
| `READY` | `ACTIVE` | `COORDINATOR` |
| `ACTIVE` | `COMPLETED` | `COORDINATOR` / `OS` — `OS` only when completion is deterministically derived from prerequisites |
| `ACTIVE` | `SUPERSEDED` | `COORDINATOR` |

`DRAFT → SUPERSEDED` and `READY → SUPERSEDED` are **not** permitted in Foundation v1.

| | |
| :--- | :--- |
| **Existing architectural requirement** | The Coordinator owns the Feature Plan (DS-009). Plan lifecycle `DRAFT → READY → ACTIVE → COMPLETED` (DS-009). A plan is revised into a new revision rather than silently overwritten; complete planning history is preserved (DS-009). |
| **Clarification** | Requester authority per transition; `OS` co-authority on derivable completion; `SUPERSEDED` is entered only from `ACTIVE`. |
| **Explicit deferral** | Supersession of a `DRAFT` or `READY` plan is excluded from Foundation v1. |

**Rationale.** DS-009's scope-change example depicts revision during active work, which is the scenario `ACTIVE → SUPERSEDED` serves. `OS` co-authority on completion mirrors Blueprint §5.1's `IN_PROGRESS → IN_VALIDATION` (`COORDINATOR` / `OS`). Widening authority later is additive.

**Affects.** Blueprint §5 (new §5.4 — the plan lifecycle is currently absent from §5). No code change — already implemented this way.

---

### 3.5 Work Package Single-Authority Projection Model

**Decision.** The Work Package lifecycle uses a single authority model.

- `DRAFT → SUBMITTED` — initiator `WORKER`.
- **All subsequent transitions are OS projections** of the corresponding Task lifecycle outcome, with initiator `OS`.

Reviewer, QA, and Coordinator hold **no independent authority over Work Package state**. The actor performs the action at the Task level; the OS maintains the Work Package projection.

| Work Package edge | Initiator | Driven by |
| :--- | :--- | :--- |
| `DRAFT` → `SUBMITTED` | `WORKER` | Task `IN_PROGRESS → SUBMITTED` |
| `SUBMITTED` → `VALIDATED` | `OS` | OS Stage 1 mandatory-evidence validation (DS-005) |
| `VALIDATED` → `REVIEWED` | `OS` | Task `IN_REVIEW → IN_QA` / `→ REVISION_REQUIRED` |
| `REVIEWED` → `ACCEPTED` | `OS` | Task `IN_QA → ACCEPTED` |
| `REVIEWED` → `REJECTED` | `OS` | Task `→ REVISION_REQUIRED` |

| | |
| :--- | :--- |
| **Existing architectural requirement** | The Worker submits the Work Package (Blueprint §5.2, §6; DS-003). A submitted Work Package is immutable and no actor may edit it; fixes require a new Revision and a new Work Package (Blueprint §5.3; DS-003). Evidence verification is hybrid: OS Stage 1, Reviewer Stage 2 (DS-005). |
| **Clarification** | Blueprint §5.3 gains an authority table. After `SUBMITTED`, Work Package state is an OS projection rather than an independently actor-requested transition. |
| **Explicit deferral** | None. |

**Rationale.** Assigning per-role authority to Work Package transitions would create a second authority model competing with the Task state machine for the same real-world actions. Keeping authority defined in exactly one place is a direct consequence of the OS being the enforcement layer. Starting narrow is safe: widening authority is additive, narrowing it is breaking.

**Affects.** Blueprint §5.3. Requires code change.

---

### 3.6 QA Severity, Priority, and Defect Status

**Decision.** `QADefect.severity` and `QADefect.priority` remain non-empty free-text labels. No taxonomy is introduced. `QADefect.status` is enumerated as `OPEN` / `RESOLVED`.

| | |
| :--- | :--- |
| **Existing architectural requirement** | QA assigns an initial Severity and Priority; **the exact Severity/Priority classification system is still open** (DS-004). Defects carry `severity`, `priority`, `is_blocker`, `status` (Blueprint §4.1 #7). Feature Acceptance requires zero unresolved in-scope defects (DS-009). |
| **Clarification** | `status` is enumerated as `OPEN` / `RESOLVED`, which is the minimum vocabulary that makes "unresolved" machine-computable. |
| **Explicit deferral** | The severity/priority taxonomy remains open, exactly as DS-004 left it. |

**Rationale.** OS enforcement needs only two machine-readable facts: whether a defect blocks, and whether it is unresolved. Severity and priority feed Coordinator remediation prioritisation (DS-009), which is agent judgement, not deterministic enforcement. Inventing a scale now would close a question the architecture deliberately left open, against ADR-002's "Complexity Must Be Earned".

**Affects.** Blueprint §4.1 #7. No code change — already implemented this way.

---

### 3.7 Claim Type Descriptive; Evidence Keyed By Capability

**Decision.** `Claim.claim_type` remains a non-empty descriptive label, not an enumeration. QA `TestResult` remains minimal. **Mandatory System Evidence rules are keyed by Task/Worker capability, not by `claim_type`.**

| | |
| :--- | :--- |
| **Existing architectural requirement** | Evidence requirements are defined by the OS; Workers do not decide the minimum required Evidence; **every Worker type follows predefined Evidence Standards** (DS-005). Reviewers apply engineering judgement to whether evidence supports the claims — Stage 2 (DS-005). The exact QA Report schema and serialised format is a future implementation concern (DS-009). |
| **Clarification** | `claim_type` never feeds a deterministic rule; it serves Reviewer Stage 2 judgement. The Checkpoint 3 `SystemEvidenceRule` must therefore be keyed off `Task.capability`. |
| **Explicit deferral** | The full QA Report schema remains deferred, as already stated by DS-009. |

**Affects.** Blueprint §4.1 #5, §4.1 #7, §15 (Checkpoint 3 description). No code change.

---

### 3.8 Plan-Local TaskDefinition Key

**Decision.** `TaskDefinition` carries a plan-local `key`. Dependencies between planned tasks reference these keys.

| | |
| :--- | :--- |
| **Existing architectural requirement** | The Feature Plan defines the task breakdown and dependencies between Tasks (DS-009); `task_definitions` carry dependency graphs (Blueprint §4.1 #2). The Coordinator creates Tasks only when their dependencies are satisfied; Tasks are not created simply because they may be needed later (DS-007). |
| **Clarification** | Dependency edges reference plan-local keys because OS `TaskId` values do not exist at planning time. |
| **Explicit deferral** | None. |

**Rationale.** Pre-allocating real Task identities during planning would conflict with DS-007's dependency-driven task creation.

**Affects.** Blueprint §4.1 #2. No code change — already implemented this way.

---

### 3.9 Actor Capabilities

**Decision.** `Actor` carries `capabilities`. A Worker must declare at least one capability. Task assignment requires capability matching.

| | |
| :--- | :--- |
| **Existing architectural requirement** | Workers are selected based on required capabilities (DS-002). `READY → ASSIGNED` requires that the Worker ID is active and possesses matching capability; the rejection triggers are worker inactive, nonexistent, or wrong capability (Blueprint §5.2). |
| **Clarification** | The capability set lives on `Actor`. Blueprint §4.1 #9's omission of the field is corrected; without it the §5.2 rule is unenforceable. |
| **Explicit deferral** | None. |

**Affects.** Blueprint §4.1 #9. No code change — already implemented this way.

---

### 3.10 Coordinator Lifecycle & Domain Registry Deferred

**Decision.** Foundation v1 does **not** implement the Coordinator lifecycle or the Domain Registry. Foundation v1 uses `Actor.is_active` only, where `is_active = True` corresponds to DS-009 `ACTIVE`. Foundation v1 may use the seeded Coordinator described in the vertical slice (Blueprint §10.2, step 2).

| | |
| :--- | :--- |
| **Existing architectural requirement** | The OS maintains an authoritative Domain Registry mapping domains to Coordinators. The Coordinator lifecycle is `PROPOSED → APPROVED → ACTIVE → SUSPENDED → RETIRED`. A `RETIRED` Coordinator never receives new Tasks or Features, while its historical records remain permanently accessible. During the initial operational phase, **Builder approval is required** before a proposed Coordinator becomes `ACTIVE` (DS-009). **All of this remains authoritative and is unchanged by this ADR.** |
| **Clarification** | The Blueprint's `Actor.is_active` is a Foundation v1 simplification, not a replacement for the DS-009 lifecycle. |
| **Explicit deferral** | The five-state Coordinator lifecycle, the Domain Registry, and the Builder-approval registration workflow are deferred from Foundation v1. |

**This is an explicit deferral, not a rejection of the architecture.**

**Constraint.** The full Coordinator lifecycle **must be resolved before the Domain Registry is implemented**. No `coordinators` or Domain Registry persistence may be created until it is.

**Rationale.** The v1 vertical slice seeds exactly one Coordinator by hand, so the proposal/approval workflow has no v1 consumer, and a five-state lifecycle with no registry to attach it to would be over-design under ADR-002. The deferral is recorded because `is_active` cannot express the Builder-approval gate — an authority boundary — and an unrecorded deferral of an authority boundary is indistinguishable from a lost requirement.

**Affects.** Blueprint §4.1 #9, §14, §15 (Checkpoint 4 precondition). No code change.

---

## Decisions — Builder Review Of 2026-08-25

The four rulings below were made after Checkpoint 2, in the Builder review of the questions §3.1–§3.10 left explicitly undecided. They use the same three-part frame. They are **clarifications, not new architecture**, and they convert no deferred capability into a Foundation v1 requirement.

### 3.11 QA Defect Scope — Verified Structural Association, Never A Declared Boolean

**Decision.** A QA defect's acceptance impact is **derived by the OS from an explicit structural association**. The OS never accepts an `in_scope = true` declaration from QA.

Every `QADefect` carries a scope association referencing either:

- the **Task** the defect was found against, or
- the **Feature** directly, when no Task represents the affected capability.

The OS resolves the association and validates the chain:

```
Defect ──→ Task ──→ Feature        (defect associated with a Task)
Defect ──────────→ Feature        (defect associated with the Feature directly)
```

The referenced entity must exist, and the chain must resolve to the Feature under validation. Scope is **derived**; it is never stored as a QA-supplied boolean.

**Unresolvable scope.** If the association is **absent**, or **resolves to no entity**, the defect's scope is **unresolved**. The OS does not guess. A QA Report marked `is_final_pass` that carries any scope-unresolved defect is **not a valid QA Final Pass**, and any Feature Acceptance depending on it is rejected with an explicit reason naming the defect, following the reject-and-explain model of DS-009. Resolution comes from QA supplying a valid association — never from OS inference.

> [!IMPORTANT]
> **Amended 2026-08-29 — Builder ruling.** As originally recorded, this paragraph also treated an association resolving to a **different Feature** as unresolved, which directly contradicted the *Out-of-scope defects* paragraph below. **The Builder has resolved the contradiction in favour of the out-of-scope reading:** a defect resolving to a different Feature is **out of scope for the Feature being accepted and does not block it**. Only genuinely unresolved scope — no association, or an association resolving to no entity — blocks acceptance. See [ADR-004 4.14](ADR-004.md#414-different-feature-is-out-of-scope--adr-003-311-contradiction-resolved). The rest of this ruling is unchanged.

**Boundary — what the OS does not determine.** The OS enforces the **integrity of the relationship**, not the semantic correctness of the judgement behind it. `Feature.in_scope` / `out_of_scope` remain free-text lists (DS-009), and the OS makes no attempt to text-match a defect against them. Deciding *which* Task or Feature a defect belongs to remains QA judgement, resolvable by the Coordinator under the DS-009 disagreement path. What changes is that the judgement is now expressed as a **checkable relationship** rather than an unverifiable flag.

**Out-of-scope defects.** A defect resolving to a different Feature is permanently recorded and simply does not block *this* Feature's acceptance. This preserves both halves of DS-009: work outside the recorded scope is never silently treated as a missing requirement, and never silently discarded either.

| | |
| :--- | :--- |
| **Existing architectural requirement** | Feature Acceptance requires zero unresolved **in-scope** defects; a defect in an in-scope capability blocks acceptance; a capability outside the recorded scope is never silently treated as a defect (DS-009). Agents may declare work ready, but only the OS determines whether objective transition criteria are met (DS-009). |
| **Clarification** | How "in scope" becomes machine-computable: a validated structural association, resolved by the OS, replacing any declared boolean. Scope-unresolved defects invalidate a QA Final Pass. |
| **Explicit deferral** | The severity / priority taxonomy remains open (D-5, unchanged). The OS still performs no semantic scope matching against `Feature.in_scope`. |

**Rationale.** `in_scope = true` is a self-assessment, and DS-009 forbids treating self-assessment as independent validation. A structural association is the weakest mechanism that the OS can actually verify: it checks existence and resolution, which are deterministic, while leaving the engineering judgement where the architecture already puts it.

**Affects.** Blueprint §4.1 #7, §5.1, §14.1 item 5, §15 (Checkpoints 2, 3, 4). **Requires code change** — `domain/qa.py` and, at Checkpoint 3, `QAInScopeZeroDefectRule`.

---

### 3.12 Task Creation Timing vs Execution Authorization

**Decision.** Tasks **may be created while the Feature Plan is still `DRAFT`**. Existence confers no authority:

> **"A Task exists" is not "a Task is authorized to execute."**

The recorded model is:

```
Feature
  ↓
Feature Plan DRAFT
  ↓
Coordinator defines Tasks
  ↓
Tasks exist as planned work        (no execution authority)
  ↓
Feature Plan READY
  ↓
Feature Plan ACTIVE
  ↓
Tasks become executable, subject to their own lifecycle and dependencies
```

**Where the gate sits.** A Task created under a non-`ACTIVE` plan rests in `CREATED` or `PENDING_DEPENDENCIES`. The OS transition **`CREATED` / `PENDING_DEPENDENCIES` -> `READY`** gains an additional precondition: **the originating Feature Plan is `ACTIVE`**. `READY` therefore means *authorized and eligible for assignment*, and every downstream state is unreachable by construction. Authorization is enforced at exactly one edge.

**Linkage.** For the gate to be computable, a `Task` records its **originating Feature Plan** (`feature_plan_id`) and the **plan-local definition key** it was created from (`plan_definition_key`, per 3.8). Without that link the OS cannot evaluate plan status, exactly as the §5.2 assignment rule was unenforceable before 3.9 placed `capabilities` on `Actor`.

**Activation reconciles.** At `READY -> ACTIVE`, the OS instantiates any plan `TaskDefinition` that has no Task yet. Both creation paths therefore converge on the same post-activation state, and Blueprint §5.1's requirement that Tasks are instantiated in the OS remains true.

**Relationship to DS-007.** **DS-007 is not modified.** Its requirement — that Tasks are created dependency-driven and not speculatively — was written to prevent work being *worked* before its prerequisites were satisfied and its shape was real. That protection is preserved in full by the authorization gate and by `PENDING_DEPENDENCIES`. DS-007 constrains when work becomes **executable**; this ruling clarifies that the **planning record** may be written earlier. This is what reconciles DS-007 with Blueprint §5.1/§5.2, which the Checkpoint 2 record flagged as describing different instantiation timings.

**Planning history is preserved.** Tasks created during planning remain permanent record even if the plan never activates. Nothing is deleted. What *terminates* such a Task is deferred (D-6).

| | |
| :--- | :--- |
| **Existing architectural requirement** | The Coordinator identifies dependencies and creates Tasks; task creation is dependency-driven (DS-007). The Coordinator owns the Feature Plan and its `DRAFT -> READY -> ACTIVE -> COMPLETED` lifecycle (DS-009). Complete planning history is preserved (DS-009). Tasks are instantiated at plan activation, with `PENDING_DEPENDENCIES` available to park them (Blueprint §5.1, §5.2). |
| **Clarification** | Task creation and execution authorization are separate events. Creation is permitted from `DRAFT`; authorization derives from the plan being `ACTIVE` and is enforced on `-> READY`. `Task` records its originating plan and definition key. |
| **Explicit deferral** | The terminal handling of a Task whose plan never activates (D-6). |

**Rationale.** Deriving authority from plan status rather than from the existence of a row keeps a single authority model — the same principle 3.5 applied to the Work Package. It also means the planning record can be as complete as the Coordinator needs it to be without any of it becoming executable early.

**Affects.** Blueprint §4.1 #2, §4.1 #3, §5.1, §5.2, §5.4, §6, §10.2, §14.1 item 8, §15 (Checkpoints 2, 4, 6). **Requires code change** — `domain/task.py` and `state/task_sm.py`, enforced by the Checkpoint 6 transition runner.

---

### 3.13 Post-Supersession Routing — Explicit Coordinator Disposition, No Automatic Migration

**Decision.** When a Feature Plan transitions `ACTIVE -> SUPERSEDED`, the OS performs **no automatic routing whatsoever**.

- The superseded Plan and its Tasks remain **immutable history**.
- Existing Tasks are **not** moved to the successor plan.
- Existing Tasks are **not** deleted.
- The OS **does not decide** what happens to unfinished work.

The **Coordinator explicitly decides the disposition of each unfinished Task**, using the vocabulary DS-008 already established for work invalidated by a higher-level decision: **resume, reuse, abandon, or redirect**. Useful work may be **represented** as new planned work in the successor plan; it is never migrated. Work no longer required may be stopped according to the eventual lifecycle rules (D-6). **No new automatic migration mechanism is introduced.**

**What follows without any new mechanism:**

1. A Task of a superseded plan that had not yet reached `READY` **simply never becomes authorized**. This is the 3.12 gate declining to act, not the OS deciding the Task's fate; it waits for Coordinator disposition.
2. A Task already in execution is **not interrupted by the OS**. It continues under its own lifecycle until the Coordinator dispositions it. **Foundation v1 has no mechanism to halt in-flight work** — that is deferral **D-1** (`BLOCKED`, DS-008), which is unchanged and remains owed.

| | |
| :--- | :--- |
| **Existing architectural requirement** | A plan is revised into a new revision rather than silently overwritten, and complete planning history is preserved (DS-009). History is strictly additive; the system never overwrites, mutates, or discards historical records (DS-009). The Coordinator determines whether invalidated work resumes, is reused, is abandoned, or is redirected (DS-008). |
| **Clarification** | Supersession triggers no OS-initiated routing of Tasks. Disposition is an explicit Coordinator act. Unauthorized Tasks remain unauthorized; in-flight Tasks are not interrupted. |
| **Explicit deferral** | The persisted record of a disposition decision, any recorded relationship between a superseded plan's Task and successor planned work, and any terminal state for stopped work (**D-6**). The mechanism to halt in-flight work (**D-1**). |

**Not decided here.** What happens to the **Feature's own lifecycle status** when its plan is superseded, and how the successor plan revision is instantiated, are **not resolved by this ruling** and remain open. See [Questions Not Decided In The Checkpoint 2 Record](#questions-not-decided-in-the-checkpoint-2-record), item 3.

**Rationale.** Any automatic rule here — migrate, cancel, halt — would be the OS making an engineering judgement about unfinished work, which is Coordinator authority under DS-007 and DS-008. Taking no action is the only behaviour that is both correct and implementable today, and it costs nothing to widen later.

**Affects.** Blueprint §5.4, §14.1 item 9, §15 (Checkpoint 6). **No code change** — Foundation v1's correct behaviour is to take no automatic action.

---

### 3.14 ADR-001 Formal Amendment Performed

**Decision.** The formal ADR-001 supersession/amendment that DS-009 declared owed, and that this ADR recorded as still outstanding, **is performed in [ADR-001](ADR-001.md) itself** as an appended Amendment section. It records that:

- The **principle** — work is not done until independently verified and accepted — remains authoritative and unchanged.
- The **original illustrative linear workflow** (`Worker -> Evidence Collection -> Review -> QA -> Human Approval -> Merge`) is superseded.
- The current architecture is defined by **Design Sessions 001–009, ADR-002, ADR-003, and the Implementation Blueprint**.
- The amendment is a **formal clarification and partial supersession, not a new architecture**.

ADR-001's original text is preserved. Only its Status, the label on the workflow block, and the appended Amendment section change.

| | |
| :--- | :--- |
| **Existing architectural requirement** | DS-009 already ruled the original ADR-001 workflow "refined and superseded" by the architecture of Sessions 001–009, and recorded that a formal ADR supersession/amendment would be documented in a subsequent checkpoint. |
| **Clarification** | The amendment's standing, its exact scope (principle retained, workflow superseded), and its location (ADR-001 itself). |
| **Explicit deferral** | None. |

**Rationale.** The notice has to sit where the misreading would occur. A separate ADR would leave ADR-001 reading as current architecture to any agent that opens it first, which is precisely the failure this ruling exists to prevent.

**Affects.** [ADR-001](ADR-001.md); the Relationship section of this ADR. **No code change.**

---

## Consequences

### Positive

- The append-only history invariant is now structurally enforced rather than merely asserted: no recorded Task Revision is ever written to after insertion (3.1).
- Authority is defined in exactly one place per real-world action. The Work Package no longer carries a competing authority model (3.5).
- Two internal Blueprint contradictions (`BLOCKED`, `Actor.capabilities`) are identified and resolved rather than silently absorbed into code.
- Every deferral is named, justified, and given a gating condition, so nothing is lost by omission.
- No taxonomy, state, or entity was invented to close a question the architecture deliberately left open.
- A defect's acceptance impact is a **verified relationship** rather than a self-declared boolean, so QA cannot assert a scope the OS has not checked (3.11).
- Task existence and execution authority are separated, so preserving planning history no longer implies authorising work (3.12).

### Trade-offs

- **DS-008's requirement that the OS prevent Workers continuing knowingly invalid work is unserved in Foundation v1** (3.3). This is the most significant consequence of this ADR and is accepted deliberately.
- **The Builder-approval gate on Coordinator activation is not enforced in Foundation v1** (3.10). With a single seeded Coordinator this has no practical effect, but it is an authority boundary that exists on paper and not in code until the deferral is closed.
- `Task.active_revision_number` becomes the single source of truth for active-ness. Persistence (Checkpoint 4) must keep it consistent with revision history; the domain layer exposes a consistency check but cannot enforce it across aggregates (3.1).
- Severity and priority are not queryable in any structured way until a taxonomy is chosen (3.6).
- **A QA Final Pass can now be invalid on scope grounds alone** (3.11). Every defect must carry a resolvable association, so a QA agent that omits one blocks Feature Acceptance until it is supplied. This is a real added obligation on QA, accepted deliberately in exchange for removing the unverified boolean.
- Foundation v1 still has **no mechanism to halt in-flight work** when a Feature Plan is superseded, and no place to record the Coordinator's disposition of it (3.13). Both remain owed under D-1 and D-6.

### Implementation impact

Only two of the ten decisions change code written during Checkpoint 2:

| Decision | Impact |
| :--- | :--- |
| 3.1 Task Revision Status | `domain/enums.py`, `domain/task.py`, `domain/__init__.py` and the corresponding revision-history tests |
| 3.5 Work Package authority | `state/work_package_sm.py` and the corresponding state machine tests |

Decisions 3.2, 3.4, 3.6, 3.7, 3.8, and 3.9 confirm the existing implementation. Decisions 3.3 and 3.10 are deferrals with documentation-only impact.

The Implementation Blueprint requires updates in §4.1 (#2, #4, #5, #7, #9, and a new entity), §5.2, §5.3, a new §5.4, §6, §7.1, §14, and §15.

**Rulings 3.11–3.14 (2026-08-25).** These postdate Checkpoint 2 and are therefore not reflected in the code written during it. Two of them require **domain fields that Checkpoint 2 does not have**:

| Decision | Impact |
| :--- | :--- |
| 3.11 QA defect scope | `domain/qa.py` — `QADefect` gains its scope association and derived scope resolution; `rules/` consumes it at Checkpoint 3 |
| 3.12 Task creation timing | `domain/task.py` — `Task` gains `feature_plan_id` and `plan_definition_key`; `state/task_sm.py` and the Checkpoint 6 transition runner gain the plan-`ACTIVE` precondition on `-> READY` |
| 3.13 Post-supersession routing | None. Foundation v1's correct behaviour is to take no automatic action |
| 3.14 ADR-001 amendment | None. Documentation only |

These land as a **Checkpoint 2 revisit performed when Checkpoint 3 begins**, not as work inside Checkpoint 2's completed record. The Blueprint additionally requires updates in §4.1 (#2, #3, #7), §5.1, §5.2, §5.4, §6, §10.2, §14.1, and §15 (Checkpoints 2, 3, 4, and 6).

---

## Explicit Deferred Capabilities

The following are **deferred from Foundation v1, not rejected**. Each remains part of the architecture of record.

| ID | Deferred capability | Source of the requirement | Gating condition |
| :--- | :--- | :--- | :--- |
| **D-1** | Escalation blocking (`BLOCKED` lifecycle state) | DS-008; Blueprint §6 | Must be designed explicitly — entry states, exit states, initiating authority — before any implementation |
| **D-2** | Coordinator lifecycle `PROPOSED → APPROVED → ACTIVE → SUSPENDED → RETIRED` | DS-009 | Must be resolved **before** the Domain Registry is implemented |
| **D-3** | Domain Registry and the Builder-approval registration workflow | DS-009 | Same as D-2 |
| **D-4** | Feature Plan supersession from `DRAFT` or `READY` | Inferred from DS-009 plan revision | No gate set; not required by Foundation v1 |
| **D-5** | QA severity / priority classification taxonomy | DS-004 (explicitly left open) | No gate set; not required by Foundation v1 |
| **D-6** | Task stop / abandon lifecycle, and the persisted record of a Coordinator disposition decision — including any recorded relationship between a superseded plan's Task and successor planned work | Builder ruling 3.13 ("stopped according to the eventual lifecycle rules") | Must be designed explicitly before any post-supersession disposition is implemented; relevant to Checkpoint 6 |

---

## Questions Not Decided In The Checkpoint 2 Record

These were surfaced during Checkpoint 2 and were **not** resolved by the Checkpoint 2 rulings (§3.1–§3.10). The original problem statements are preserved verbatim so the reasoning that produced the later rulings is not lost. Current status is marked inline.

1. **In-scope defect identification.** DS-009 gates Feature Acceptance on "zero unresolved **in-scope** defects", but `QADefect` carries no in-scope marker. The Checkpoint 3 acceptance rule cannot be written deterministically until this is resolved. **Must be resolved before Checkpoint 3.**
   - **RESOLVED 2026-08-25 by [§3.11](#311-qa-defect-scope--verified-structural-association-never-a-declared-boolean).** Scope is derived from a validated structural association (`Defect -> Task -> Feature`), never from a declared boolean.
2. **Task instantiation timing.** DS-007 states Tasks are created only when their dependencies are satisfied; Blueprint §5.1 requires Tasks to be instantiated at plan activation, with §5.2 providing `PENDING_DEPENDENCIES` to park them. **Must be resolved before Checkpoint 6.**
   - **RESOLVED 2026-08-25 by [§3.12](#312-task-creation-timing-vs-execution-authorization).** Tasks may be created from `DRAFT`; execution authority derives from the plan being `ACTIVE` and is enforced on `-> READY`.
3. **Post-supersession routing.** DS-009 does not state what happens to a Feature when its plan is superseded, or how the successor plan revision is instantiated. Relevant to Checkpoint 6.
   - **RESOLVED IN PART 2026-08-25 by [§3.13](#313-post-supersession-routing--explicit-coordinator-disposition-no-automatic-migration).** The disposition of the superseded Plan and its Tasks is resolved: no automatic migration, deletion, or halting; the Coordinator decides explicitly.
   - **STILL OPEN.** What happens to the **Feature's own lifecycle status** when its plan is superseded, and how the successor plan revision is instantiated, are not decided. Relevant to Checkpoint 6.

---

## Relationship to Design Sessions 001–009, ADR-001, and ADR-002

**This ADR does not supersede any prior architectural record.**

| Record | Status after ADR-003 |
| :--- | :--- |
| Design Sessions 001–009 | **Authoritative and frozen. Unchanged.** |
| ADR-001 | **Authoritative. Principle unchanged.** Its original illustrative linear workflow is formally superseded by the [ADR-001 Amendment](ADR-001.md#amendment--formal-clarification--partial-supersession-2026-08-25), performed under §3.14. |
| ADR-002 | **Authoritative. Unchanged.** |
| Implementation Blueprint | Requires the clarifying updates listed under Consequences |

Specifically:

- **DS-006** is not modified. Its requirement that only one Revision is active at any point in time is preserved as a derived invariant (3.1). Its requirement that history is never rewritten is what motivated removing the stored status.
- **DS-008** is not modified. Its escalation-blocking requirement is deferred, not withdrawn (3.3, D-1).
- **DS-009** is not modified. Its Coordinator lifecycle, Domain Registry, and Builder-approval policy remain the architecture of record and are deferred, not replaced (3.10, D-2, D-3).
- **DS-004**'s open Severity/Priority question remains open (3.6, D-5).
- **DS-005**'s Evidence Standards model — OS-defined, per Worker type — is the basis for keying evidence rules by capability (3.7).
- **ADR-001**'s independent verification principle is reinforced, not altered: the Work Package projection model (3.5) removes the possibility of an actor advancing a Work Package outside the verification stages defined by the Task lifecycle.
- **ADR-002**'s "Complexity Must Be Earned" is the operative principle behind every deferral recorded here.

**Note (Checkpoint 2 record).** DS-009 records that a formal ADR supersession/amendment of ADR-001's original linear workflow is owed in a subsequent checkpoint. **ADR-003 is not that record and does not perform that amendment.** It remains outstanding.

**Update (2026-08-25).** The amendment has since been performed, in ADR-001 itself, under the Builder ruling recorded at [§3.14](#314-adr-001-formal-amendment-performed). The note above stands as the Checkpoint 2 record and is no longer current.

---

## Foundation v1 Scope Boundary

Confirmed **in scope** for Foundation v1, as clarified by this ADR:

- Feature, Feature Plan, Task, Task Revision, Work Package, Evidence, QA Report, Decision, Review Decision, and Actor as pure domain concepts.
- Deterministic lifecycle state machines for Feature, Feature Plan, Task, and Work Package, with explicit per-transition authority.
- Append-only Task Revision history with no post-insertion writes.
- Immutable submitted Work Packages.
- Capability-matched Task assignment.
- A single hand-seeded Coordinator, per Blueprint §10.2.
- QA defect scope recorded as a **verified structural association** and derived by the OS, never declared (3.11).
- Task existence separated from execution authorization, gated on Feature Plan activation (3.12).

Confirmed **out of scope** for Foundation v1:

- Escalation blocking and the `BLOCKED` state (D-1).
- The Coordinator lifecycle, the Domain Registry, and Builder-approval registration (D-2, D-3).
- Feature Plan supersession from `DRAFT` or `READY` (D-4).
- A severity/priority taxonomy (D-5).
- Any independent Reviewer/QA/Coordinator authority over Work Package state (3.5).
- Automatic migration, deletion, or halting of a superseded plan's Tasks (3.13).
- A Task stop/abandon terminal state and any persisted disposition record (D-6).

Nothing in this ADR converts a deferred capability into a Foundation v1 requirement.
